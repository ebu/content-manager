<UserControl x:Class="ContentManager.GUI.Controls.CartPreview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="114" d:DesignWidth="596">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80*"/>
            <ColumnDefinition Width="100"/>
        </Grid.ColumnDefinitions>
        <StackPanel x:Name="stack" Orientation="Horizontal"></StackPanel>

        <WrapPanel Grid.Column="1">
            <ComboBox x:Name="availableSlides" Width="100"/>
            <Button Content="Add" Click="Button_Click" />
            <ToggleButton Content="Remove"/>
        </WrapPanel>
    </Grid>
</UserControl>
